# PlatformIO / build
.pio
.cache
compile_commands.json

# Node / µGulp
node_modules/
.microgulp/
npm-debug.log*
package-lock.json.bak

# Local NAS targets (never commit real paths)
config/nas.targets.local
.env
.env.*
!.env.example
!.env.*.example

# Secrets / credentials / private keys
*.pem
*.key
*.p12
*.pfx
*credentials*
*secrets*
.settings_private*
settings_private.*

# IDE caches
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/*.log
.idea/
*.swp
*~

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Local refs / vendor dumps (not project source)
*_refs
_refs/

# Transient logs
*_log.txt
build_log.txt
upload_log.txt
monitor_log.txt
pkg_install_log.txt

# ESP-IDF / PlatformIO generated sdkconfig (per-env + defaults copy)
sdkconfig
sdkconfig.old
sdkconfig.defaults
sdkconfig.t-lion-idf-spike
sdkconfig.t-lion-idf-spike-classic
sdkconfig.t-lion-idf-spike-ble
sdkconfig.t-lion-idf-debug
sdkconfig.t-lion-idf-release

# Python
*.pyc
__pycache__/

# clangd / tooling
.clangd
.cache/
